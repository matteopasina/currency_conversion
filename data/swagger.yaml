type: object
required:
- to_currency
- amounts
properties:
  to_currency:
    type: string
  amounts:
    type: array
    items:
      type: object
      required:
      - from_currency
      - amounts
      properties:
        amounts:
          type: array
          items:
            type: string
        from_currency:
          type: string
